<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="test">
  <select id="login" parameterType="com.java.web.Login" resultType="com.java.web.Login">
    select id,pw from login where id=#{id} and pw=#{pw,jdbcType=VARCHAR}
  </select>
   <select id="contentall" resultType="int">
   select count(*) from content where delYn='N'
  </select>
    
   <select id="content" resultType="com.java.web.Bean">
   select no,title,val,writer from (select ROWNUM numrow,no,title,val,writer from content where delYn='N') where numrow <![CDATA[ > ]]>= (#{pageNum}-1)*3+1 and numrow <![CDATA[ < ]]>= (#{pageNum})*3
  </select>
   <select id="finalno" resultType="int">
   	select MAX(no) from content
  </select>
  <insert id="insert" parameterType="String">
  	insert into content (no,title,val,writer) values (tmp_seq.NEXTVAL,#{title},#{val,jdbcType=VARCHAR},#{writer})
  </insert>
  <update id="update" parameterType="com.java.web.Bean">
  	update content set val=#{val,jdbcType=VARCHAR} where no=#{no,jdbcType=VARCHAR}
  </update>
  
  <update id="delete" parameterType="com.java.web.Bean">
  	update content set delYn='Y' where no=#{no,jdbcType=VARCHAR}
  </update>
  <insert id="insertFile" parameterType="com.java.web.FileBean">
  	insert into fileinfo (no,filename,fileurl,ext) values (#{no},#{filename},#{fileurl,jdbcType=VARCHAR},#{ext,jdbcType=VARCHAR})
  </insert>
  <select id="selectFile" parameterType="int" resultType="com.java.web.FileBean">
  	select no,filename,fileURL,ext from fileinfo where no = #{no}
  </select>
    <select id="selectFileOne" parameterType="com.java.web.FileBean" resultType="com.java.web.FileBean">
  	select no,filename,fileURL,ext from fileinfo where no = #{no} and filename=#{filename}
  </select>
  
   <select id="deleteFile" parameterType="int">
  	delete from fileinfo where no = #{no}
  </select>
  
  <select id="detail" parameterType="int" resultType="com.java.web.Bean">
 	 select no,title,val,writer from content where no=#{no} and delYn='N'
  </select>
  
  <update id="updateDetail" parameterType="com.java.web.Bean" >
  	update content set title=#{title}, val=#{val}  where no=#{no} and delYn='N'
  </update>
  
  <update id="deleteDetail" parameterType="com.java.web.Bean" >
	update content set delYn='Y' where no=#{no}
  </update>
  
</mapper>